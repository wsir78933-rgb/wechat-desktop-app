/**
 * 窗口模块统一导出
 */

// 导出主窗口
export { MainWindow, mainWindow } from './mainWindow';

// 导出悬浮窗
export { FloatWindow, floatWindow } from './floatWindow';

// 导出窗口管理器
export { WindowManager, windowManager } from './windowManager';
